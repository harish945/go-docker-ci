name: CI/CD

on:
  push:
    branches: [main, master, dev]
  pull_request:
    branches: [main, master, dev]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Build
        run: |
          echo "Building Go application..."
          go build -o main .
          echo "Build successful"

  deploy-dev:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/dev'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log - Start Deploy Dev
        run: echo "Starting DEV deployment on branch ${{ github.ref }}"

      - name: Check Secrets Set
        run: |
          if [ -z "${{ vars.DOCKER_USERNAME }}" ]; then echo "DOCKER_USERNAME is EMPTY"; else echo "DOCKER_USERNAME is set"; fi
          if [ -z "${{ vars.DOCKER_PASSWORD }}" ]; then echo "DOCKER_PASSWORD is EMPTY"; else echo "DOCKER_PASSWORD is set"; fi

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKER_USERNAME }}
          password: ${{ vars.DOCKER_PASSWORD }}

      - name: Log - Docker Login Success
        run: echo "Docker Hub login successful"

      - name: Build and Push (dev)
        run: |
          echo "Building Docker image for DEV..."
          docker build -t ${{ vars.DOCKER_USERNAME }}/go-app:dev .
          echo "Pushing image to Docker Hub..."
          docker push ${{ vars.DOCKER_USERNAME }}/go-app:dev
          echo "DEV image pushed successfully: ${{ vars.DOCKER_USERNAME }}/go-app:dev"

  deploy-prod:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log - Start Deploy Prod
        run: echo "Starting PROD deployment on branch ${{ github.ref }}"

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKER_USERNAME }}
          password: ${{ vars.DOCKER_PASSWORD }}

      - name: Log - Docker Login Success
        run: echo "Docker Hub login successful"

      - name: Build and Push (prod)
        run: |
          echo "Building Docker image for PROD..."
          docker build -t ${{ vars.DOCKER_USERNAME }}/go-app:prod .
          echo "Pushing image to Docker Hub..."
          docker push ${{ vars.DOCKER_USERNAME }}/go-app:prod
          echo "PROD image pushed successfully: ${{ vars.DOCKER_USERNAME }}/go-app:prod"
